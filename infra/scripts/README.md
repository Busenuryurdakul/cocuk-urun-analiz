# XCS — Miyuna Internal Orchestration Scripts

Project-internal automation layer (not a user-facing product).

Planned scripts (Phase 1+):

- `setup-agent-runtime.ps1` / `.sh` — GPU/RAM-based agent runtime configuration
- `provision-dev.ps1` / `.sh` — local environment provisioning
- `deploy-staging.ps1` / `.sh` — staging deploy helpers

See `docs/FINAL_MASTER_PROMPT.md` §5 (XCS).
