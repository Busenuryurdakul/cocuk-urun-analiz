package compliance

const (
	// PlatformDefaultPolicyVersion is the bootstrap published platform policy version.
	PlatformDefaultPolicyVersion = "1.0.0"
)

// PersonalDefaultComplianceProfile is the canonical default for personal workspaces (Phase 3).
const PersonalDefaultComplianceProfile = "KVKK"
